# =============================================================================
# Code Ingestion System - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# Never commit .env to git (it should be in .gitignore)

# =============================================================================
# SurrealDB Configuration (when VECTOR_BACKEND=surrealdb)
# =============================================================================
# For Docker Compose: use http://surrealdb:8000
# For local development: use http://localhost:8000
SURREALDB_URL=http://localhost:8000
SURREALDB_NS=code_ingest
SURREALDB_DB=vectors
SURREALDB_USER=root
SURREALDB_PASS=root

# =============================================================================
# Required: Embedding Service (DeepInfra)
# =============================================================================

# DeepInfra API key (required for embeddings)
# Get your API key from: https://deepinfra.com/dash/api_keys
DEEPINFRA_API_KEY=your_deepinfra_api_key_here

# Optional: Embedding model (default: Qwen/Qwen3-Embedding-8B)
# EMBEDDING_MODEL=Qwen/Qwen3-Embedding-8B

# Optional: Override DeepInfra base URL (default: https://api.deepinfra.com/v1/openai)
# DEEPINFRA_EMBEDDING_BASE_URL=https://api.deepinfra.com/v1/openai

# Optional: Chunks per embedding request (default: 15 for standard model; use 25–100 for -batch model)
# BATCH_SIZE=15

# Optional: Embedding request timeout in seconds (default: 120; standard model can be slower)
# EMBEDDING_TIMEOUT=120

# =============================================================================
# Required for Repository Cloning: GitHub Personal Access Token
# =============================================================================
# Create PAT with 'repo' scope at: https://github.com/settings/tokens
# Used to clone repositories for ingestion
GITHUB_TOKEN=your_github_pat_token_here

# =============================================================================
# Optional: Development/Testing
# =============================================================================
# Set to 'development' to enable debug logging
# ENVIRONMENT=production

# Log level for ingest container (DEBUG, INFO, WARNING, ERROR). INFO shows file/chunk/batch progress.
# LOG_LEVEL=INFO

# Override path to repository configuration file
# Default: config/repositories.yaml
# REPOSITORIES_CONFIG=config/repositories.yaml

# Override base directory for cloned repositories (if not set in repositories.yaml)
# REPOS_BASE_DIR=./repos

# Override collections configuration file path (shared between ingest and MCP)
# Default: config/collections.yaml
# COLLECTIONS_CONFIG=config/collections.yaml
