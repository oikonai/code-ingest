# =============================================================================
# Code Ingestion System - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# Never commit .env to git (it should be in .gitignore)

# =============================================================================
# Required: Vector Database (Qdrant)
# =============================================================================
QDRANT_URL=https://your-qdrant-instance.qdrant.io
QDRANT_API_KEY=your_qdrant_api_key_here

# =============================================================================
# Required: Embedding Service (Choose One)
# =============================================================================

# Option 1: Cloudflare AI Gateway + DeepInfra (Recommended)
# Get Cloudflare token from: https://dash.cloudflare.com/profile/api-tokens
CLOUDFLARE_AI_GATEWAY_TOKEN=your_cloudflare_token_here
# Get DeepInfra key from: https://deepinfra.com/dash/api_keys
DEEPINFRA_API_KEY=your_deepinfra_api_key_here

# Option 2: Modal (GPU Embedding Service)
# Get Modal credentials from: https://modal.com/settings
# MODAL_TOKEN_ID=your_modal_token_id_here
# MODAL_TOKEN_SECRET=your_modal_token_secret_here
# MODAL_ENDPOINT=https://ardaglobal--embed.modal.run

# =============================================================================
# Required for Repository Cloning: GitHub Personal Access Token
# =============================================================================
# Create PAT with 'repo' scope at: https://github.com/settings/tokens
# Used to clone repositories for ingestion
GITHUB_TOKEN=your_github_pat_token_here

# Alternative name (both are checked)
# I2P_REPO_PAT=your_github_pat_token_here

# =============================================================================
# Optional: Development/Testing
# =============================================================================
# Set to 'development' to enable debug logging
# ENVIRONMENT=production

# Override base directory for cloned repositories
# REPOS_BASE_DIR=./repos

# =============================================================================
# Notes
# =============================================================================
# Required variables (checked by 'make check-env'):
#   - QDRANT_URL
#   - QDRANT_API_KEY
#
# Optional but recommended:
#   - CLOUDFLARE_AI_GATEWAY_TOKEN + DEEPINFRA_API_KEY (for embeddings)
#   - GITHUB_TOKEN (for repository cloning)
#
# Quick setup:
#   1. Copy this file: cp .env.example .env
#   2. Fill in your credentials
#   3. Run: make check-env
#   4. Run: make setup
