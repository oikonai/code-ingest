# =============================================================================
# MCP Server Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# Never commit .env to git (it should be in .gitignore)

# =============================================================================
# Required: Vector Database Backend Selection
# =============================================================================
# Choose backend: 'qdrant' (cloud/remote) or 'surrealdb' (local/docker)
VECTOR_BACKEND=qdrant

# =============================================================================
# Qdrant Configuration (when VECTOR_BACKEND=qdrant)
# =============================================================================
QDRANT_URL=https://your-qdrant-instance.qdrant.io
QDRANT_API_KEY=your_qdrant_api_key_here

# =============================================================================
# SurrealDB Configuration (when VECTOR_BACKEND=surrealdb)
# =============================================================================
# For Docker Compose: use http://surrealdb:8000
# For local development: use http://localhost:8000
SURREALDB_URL=http://localhost:8000
SURREALDB_NS=code_ingest
SURREALDB_DB=vectors
SURREALDB_USER=root
SURREALDB_PASS=root

# =============================================================================
# Required: Embedding Service (DeepInfra)
# =============================================================================

# DeepInfra API key (required for embeddings)
# Get your API key from: https://deepinfra.com/dash/api_keys
DEEPINFRA_API_KEY=your_deepinfra_api_key_here

# Optional: Embedding model (default: Qwen/Qwen3-Embedding-8B)
EMBEDDING_MODEL=Qwen/Qwen3-Embedding-8B

# Optional: Override DeepInfra base URL (default: https://api.deepinfra.com/v1/openai)
# EMBEDDING_ENDPOINT=https://api.deepinfra.com/v1/openai

# =============================================================================
# Optional: Collections Configuration
# =============================================================================

# Path to shared collections config (YAML file defining collection names)
# This ensures MCP searches the same collections that ingest writes to
# Default: config/collections.yaml (relative to workspace root)
# COLLECTIONS_CONFIG=config/collections.yaml

# =============================================================================
# Optional: GitHub Integration
# =============================================================================

# GitHub Personal Access Token (for future features, not currently required)
# GHCR_TOKEN=your_github_token_here

# =============================================================================
# Notes
# =============================================================================
# Required variables:
#   - QDRANT_URL
#   - QDRANT_API_KEY
#   - DEEPINFRA_API_KEY
#
# Optional variables:
#   - EMBEDDING_MODEL (default: Qwen/Qwen3-Embedding-8B)
#   - EMBEDDING_ENDPOINT (override DeepInfra base URL)
#   - COLLECTIONS_CONFIG (defaults to config/collections.yaml)
#   - GHCR_TOKEN (for future GitHub features)
#
# Quick setup:
#   1. Copy this file: cp mcp/.env.example mcp/.env
#   2. Fill in your credentials
#   3. Run: python mcp/server.py
